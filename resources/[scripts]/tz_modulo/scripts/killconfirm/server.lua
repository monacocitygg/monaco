-- server-side do killconfirm
-- a deteccao de kill eh feita inteiramente no client
-- esse arquivo existe pra manter a estrutura padrao do modulo

if Config.KillConfirm.Debug then
    print('[KILL-CONFIRM] Modulo carregado (server).')
end
